description = 'An extraction extractor'

dependencies {
    //exception
    api group: 'com.legyver', name: 'core', version: legyverCoreVersion
    
    testImplementation 'junit:junit:4.13'
}

test {
    useJUnit()
}