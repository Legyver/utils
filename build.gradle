allprojects {
    
    repositories {
        mavenLocal()
        mavenCentral()
        jcenter()
    }
}

subprojects {
    ext {
        legyverCoreVersion = '1.1.0.0'
        publishBuildScript = 'https://raw.githubusercontent.com/Legyver/common-build/1.2/publish.gradle' //java 9+
    }
}