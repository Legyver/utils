allprojects {
    
    repositories {
        mavenLocal()
        mavenCentral()
        jcenter()
    }
}

subprojects {
    ext {
        legyverCoreVersion = '1.1.1.1'
        publishBuildScript = 'https://raw.githubusercontent.com/Legyver/common-build/1.3/publishLibraryModule.gradle' //java 9+ multi-module
    }
}