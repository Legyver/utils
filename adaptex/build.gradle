description = 'An extraction adapter'

dependencies {
    //exception
    api group: 'com.legyver', name: 'core', version: legyverCoreVersion
    
    testImplementation project(':extractex')
    testImplementation 'junit:junit:4.13'
}

test {
    useJUnit()
}